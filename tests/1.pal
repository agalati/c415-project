// errors on lines 25, and 38

program main(input, output);
    var n, oddcount, evencount : integer;
    procedure ODD ( x : integer );
        procedure EVEN;
        
        begin
            if x <> 0 then begin
                  evencount := evencount + 1 ;
                  x := x div 2;
                  if odd(x) then ODD ( x )
                  else EVEN
            end;
        end;
        
    begin
          oddcount := oddcount + 1;
          x := x div 2;
          if odd(x) then ODD ( x )
          else EVEN
    end;
    
    procedure EVEN ( x : integer );
          procedure „ODD ( var x : integer );       //unrecognized token attached to a procedure invok
          
          begin
                oddcount := oddcount + 1;
                x := x div 2;
                if odd(x) then ODD ( x )
                else EVEN ( x )
          end;
          
    begin
          if x <> 0 then begin
                evencount := evencount + 1;
                x := x div 2;
                if odd(x) then ODD ( xﬁ )         // unrecognized token attached to procedutr var
                else EVEN ( x )
          end;
    end;
    
    begin
    if n >= 0 then begin
          oddcount := 0;
          evencount := 0;
          if odd(n) then ODD ( n )
          else EVEN ( n );
          writeln ('n = ', n, ' o = ', oddcount, ' e = ', evencount );
end;
end.
