//errors on lines 1, 26, 36

progrm main;                          // misspelt "program" declaraction
    var n, oddcount, evencount : integer;
    procedure ODD ( x: integer );
    begin
        if x <> 0 then begin
            evencount := evencount +1;
            x := x div 2;
            if odd ( x ) then ODD ( x )
            else EVEN
            
        end;
    end;
    
    begin
        oddcount := oddcount +1;
        x := x div 2;
        if odd ( x ) then ODD ( x )
        else EVEN
    end;
    
    procedure EVEN ( x : integer );
        procedure ODD ( var x ; integer );          // type assignment ":" replaced
                                  // with semi-colon
        begin 
            oddcount := oddcount + 1 ;
            x := x div 2;
            if odd ( x ) then ODD ( x )
            else EVEN ( x )
        end;
        
    begin
        if x <> 0 then begin
            evencount := evencount + 1 + 1 + 1;     //expanded addition operation
            x := x div 2;
            if odd ( x ) then ODD ( x )
            else EVEN ( x )
        end;
    end;
    
    begin 
    if n >= 0 then begin
        oddcount := 0;
        evencount := 0;
        if odd ( n ) then ODD ( n )
        else EVEN ( n);
        writeln ('n = ', n, ' o = ', oddcount, ' e = ', evencount );
    end;
    end;                            // final end should have "." not ";"
