// errors on lines 7, 24, and 54

program main(input, output);

		label 1;
		var n, oddcount, evencount : integer;
		procedure ÓDD ( x : integer );				// unrecognized symbol in procedure declaration
				procedure EVEN;
				
				begin
						if x <> 0 then begin
									evencount := evencount + 1 ;
									x := x div 2;
									if odd(x) then ODD ( x )
									else EVEN
						end;
				end;
				
		begin
					oddcount := oddcount + 1;
					x := x div 2;
					if odd(x) then ODD ( x )
					else EVEN
					^&*											// garbage characters on newline
		end;
		
		procedure EVEN ( x : integer );
					procedure ODD ( var x : integer );
					
					begin
								oddcount := oddcount + 1;
								x := x div 2;
								if odd(x) then ODD ( x )
								else EVEN ( x )
					end;
					
		begin
					if x <> 0 then begin
								evencount := evencount + 1;
								x := x div 2;
								if odd(x) then ODD ( x )
								else EVEN ( x )
					end;
		end;
		
		begin
		1: read ( n );
		if n >= 0 then begin
					oddcount := 0;
					evencount := 0;
					if odd(n) then ODD ( n )
					else EVEN ( n );
					writeln ('n = ', n, ' o = ', oddcount, ' e = ', evencount );
					goto º1;										// unrecognize symbol in goto location
end;
end.