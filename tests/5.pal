// errors on lines 5, 28, and 33

program main;
    var n, oddcount, evencount : integer;
    procedure ODD ( x: int );     // "int" is not a reserve word, should be "integer"
    begin
        if x <> 0 then begin
            evencount := evencount +1;
            x := x div 2;
            if odd ( x ) then ODD ( x )
            else EVEN
            
        end;
    end;
    
    begin
        oddcount := oddcount +1;
        x := x div 2;
        if odd ( x ) then ODD ( x )
        else EVEN
    end;
    
    procedure EVEN ( x : integer );
        procedure ODD ( var x : integer );
        
        begin 
            oddcount := oddcount + 1 ;
            x = x div 2;        // should be assignment ":=" rather than compare "="
            if odd ( x ) then ODD ( x )
            else EVEN ( x )
        end;
        
    begin;                    // "begin" should not be followed by semi-colon
        if x <> 0 then begin
            evencount := evencount + 1;
            x := x div 2;
            if odd ( x ) then ODD ( x )
            else EVEN ( x )
        end;
    end;
    
    begin 
    if n >= 0 then begin
        oddcount := 0;
        evencount := 0;
        if odd ( n ) then ODD ( n )
        else EVEN ( n);
        writeln ('n = ', n, ' o = ', oddcount, ' e = ', evencount );
    end;
    end.
