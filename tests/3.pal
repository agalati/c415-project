// errors on lines 4, 10, and 46

program main(input, output);
    |465E?¨                                   // nonsense line with unrecognized character
    var n, oddcount, evencount : integer;
    procedure ODD ( x : integer );
        procedure EVEN();
        
        begin
            if x <> 0 then begin
                  evencount := evencount + 3.@1 ;   // unrecognized symbol in a real number
                  x := x div 2;
                  if odd(x) then ODD ( x )
                  else EVEN()
            end;
        end;
        
    begin
          oddcount := oddcount + 1;
          x := x div 2;
          if odd(x) then ODD ( x )
          else EVEN()
    end;
    
    procedure EVEN ( x : integer );
          procedure ODD ( var x : integer );
          
          begin
                oddcount := oddcount + 1;
                x := x div 2;
                if odd(x) then ODD ( x )
                else EVEN ( x )
          end;
          
    begin
          if x <> 0 then begin
                evencount := evencount + 1;
                x := x div 2;
                if odd(x) then ODD ( x )
                else EVEN ( x )
          end;
    end;
    
    begin
    if n >=< 0 then begin                         // unrecognized operator token
          oddcount := 0;
          evencount := 0;
          if odd(n) then ODD ( n )
          else EVEN ( n );
          writeln ('n = ', n, ' o = ', oddcount, ' e = ', evencount );
end;
end.
