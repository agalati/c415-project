// errors on lines 17, 19, and 34

program main;
    var n, oddcount, evencount : integer;
    procedure ODD ( x: integer );
    begin
        if x <> 0 then begin
            evencount := evencount +1;
            x := x div 2;
            if odd ( x ) then ODD ( x )
            else EVEN
            
        end;
    end;
    
    begin
        oddcount := oddcount + ?;       // "?" character is undeclared
        x := x div 2;             // and not a valid term for operation
        if odd ( x ) then ODD ( x ) then ODD ( x )  // duplicate "then" statement 
        else EVEN
    end;
    
    procedure EVEN ( x : integer );
        procedure ODD ( var x : integer );
        
        begin 
            oddcount := oddcount + 1 ;
            x := x div 2;
            if odd ( x ) then ODD ( x )
            else EVEN ( x )
        end;
        
    begin
        if x >< 0 then begin          // should be "<>" for not, will instead
            evencount := evencount + 1;   // evaluate to less than '>'
            x := x div 2;
            if odd ( x ) then ODD ( x )
            else EVEN ( x )
        end;
    end;
    
    begin 
    if n >= 0 then begin
        oddcount := 0;
        evencount := 0;
        if odd ( n ) then ODD ( n )
        else EVEN ( n);
        writeln ('n = ', n, ' o = ', oddcount, ' e = ', evencount );
    end;
    end.
