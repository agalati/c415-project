// errors on lines 13, 16, 17, 18, 28, 41, and 50

program main;
		var n, oddcount, evencount : integer;
		procedure ODD ( x: integer );
		begin
				if x <> 0 then begin
						evencount := evencount +1;
						x := x div 2;
						if odd ( x ) then ODD ( x )		
						else EVEN
						else ODD			// additional else statement
						
				end;						// three additional ends;
				end;
				end;
				end;
		end;
		
		begin
				oddcount := oddcount +1;
				x := x div 2;
				if odd ( x ) then ODD ( x )
				else EVEN
		end;
		
		begin end;							// empty begin and end block on one line
		
		procedure EVEN ( x : integer );
				procedure ODD ( var x : integer );
				
				begin 
						oddcount := oddcount + 1 ;
						x := x div 2;
						if odd ( x ) then ODD ( x )
						else EVEN ( x )
				end;
				
		begin
				if x </> 0 then begin		// added eroneous slash character to not operator
						evencount := evencount + 1;
						x := x div 2;
						if odd ( x ) then ODD ( x )
						else EVEN ( x )
				end;
		end;
		
		begin 
		if n >= 0 then begin
				oddcount := 0;
				evencount := 0;
				if odd ( n ) then ODD ( n )
				else EVEN ( n);
				writeln ('n = ', n, ' o = ', oddcount, ' e = ', evencount );
		end;
		end.
