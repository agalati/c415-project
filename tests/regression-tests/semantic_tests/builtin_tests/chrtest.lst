program main(input, output);
var
  c : char;
  b : boolean;
begin
  c := chr(98);
  c := chr(ord('c'));
  b := chr(98) = chr(ord('c'));
  chr('c');
##semantic:9: Incompatible parameter passed to 'chr' in position 1
  b := chr(0);
##semantic:10: assignment type is incompatible
end.
